server:
  port: 8080

my:
 name: jack
 age: 12
 number: ${random.int}
 uuid : ${random.uuid}
 max: ${random.int(10)}
 value: ${random.value}
 greeting: hi,i'm ${my.name}

management:
 port: 9001
 security:
 enabled: false


